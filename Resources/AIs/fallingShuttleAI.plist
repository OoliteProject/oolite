{
	GLOBAL =
	{
		ENTER = ("setSpeedFactorTo: 0.25", "setStateTo: FLY_HOME");
	};
	"FLY_HOME" =
	{
		ENTER = (setCourseToPlanet, checkCourseToDestination);
		"COURSE_OK" = ("setSpeedFactorTo: 1.0", performFlyToRangeFromDestination);
		"WAYPOINT_SET" = ("setAITo: gotoWaypointAI.plist");
		"APPROACHING_SURFACE" = (landOnPlanet);
		"DESIRED_RANGE_ACHIEVED" = (landOnPlanet);
		ATTACKED = (setTargetToPrimaryAggressor, broadcastDistressMessage, fightOrFleeHostiles);
		"INCOMING_MISSILE" = (fightOrFleeMissile);
		UPDATE = (setCourseToPlanet, "setDesiredRangeTo: 50.0", checkCourseToDestination, "pauseAI: 10.0");
	};
}
